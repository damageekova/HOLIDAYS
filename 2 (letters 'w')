#include <iostream>
using namespace std;
/*Напишите программу, которая подсчитывает, сколько раз в строке встретился символ ‘w’*/
int main (){
int n;
int i=0;
char *arr=new char[n];
cin>>arr;
int k=0;
for (i=0;i<n; i++) {
if (arr[i]=='w') k+=1;
}
cout<<k<<" letters w";
delete[] arr;
return 0;
}
